# Data directory

Raw battery files are not versioned. Either place the three `.mat` files listed in
`docs/DATA.md` here, or set the `BATTERY_DATA_DIR` environment variable to the directory
that already contains them.

Keep this file when copying or cloning the repository; all other files in this directory
are ignored by Git.

